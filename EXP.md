
### 可重复构建实验

#### 1. 实验准备
* **环境要求**：Ubuntu 20.04 / 22.04 / 25.04
* **依赖工具**：Go 1.25+、Docker、GCC 工具链、Python3.8+
* **实验样本**： `$input_path` 目录下包含 10 个项目（lz4, sqlite, TinyCC, e2fsprogs, nginx, zlib, busybox, lua, openssl, redis）及其对应的 `build_record.yaml`。

#### 2. 操作步骤
1.  **环境清理**：每次实验前，确保工作目录干净，避免旧的编译产物干扰。
2.  **数据记录**：
    * **性能**：程序将通过 `--enable_timer` 记录核心构建耗时。
    * **一致性**：记录生成的二进制产物 SHA-256 哈希值。
3.  **重复验证**：每个项目在不同操作系统环境下至少执行 3 次，以获取平均耗时。

---

#### 3. 自动化实验脚本 (`run_experiment.sh`)

这个脚本封装了指令，支持通过命令行参数传入路径，并自动化遍历多个项目进行实验。
